public class resizing {
    
    /*
    
    public static int[] resize(int[] arr, int capacity){
        int[] temp = new int [capacity];
        int x = arr.length;
        for(int i = 0; i<x ; i++){
            temp[i] = arr[i];
        }
        //return temp;                          //works directly this way too
        arr = temp;
        return arr;
    }

    public static void printArray(int[] array1){
        int n = array1.length;
        for(int i = 0; i<n;i++){
            System.out.print(array1[i]+"  ");
        }
        System.out.println();
    }


    public static void main(String[] args) {
        
        int[] there_bro = {1,2,3,4,5};
        System.out.println("Size of ariginal array: "+there_bro.length);
        there_bro = resize(there_bro, there_bro.length * 2);                   //by using this we will store the new array in origianl memory and then we can see the increased size
        System.out.println("Size of original array after resize: "+there_bro.length);    //directly using this won't increase the size it'll point towards the original only
        printArray(there_bro);

    }
    
    */

}
